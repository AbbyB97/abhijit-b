import {
  Box,
  Button,
  Flex,
  Text,
  useColorMode,
  useColorModeValue,
} from "@chakra-ui/react";
import type { NextPage } from "next";
import HomeModule from "modules/home";
const Home: NextPage = () => {
  return <HomeModule />;
};

export default Home;
